<h1><%= @student.to_s %></h1>
</p> <% if @student.active %>
    <p>This student is currently active.</p>
<% else %>
    <p>This student is currently inactive.</p>
<% end %>

<%= link_to("Toggle Active", edit_student_path(@student)) %>